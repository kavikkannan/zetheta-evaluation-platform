{
  "name": "@zetheta/ui",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "dev": "tsc -p tsconfig.json --watch --noEmit",
    "lint": "eslint src --max-warnings=0",
    "test": "echo \"No tests yet for ui\""
  },
  "devDependencies": {
    "@types/react": "^19.0.8",
    "@zetheta/shared-config": "workspace:*"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  }
}
